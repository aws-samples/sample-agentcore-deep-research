# Deep research agent dependencies with pinned versions
# Upper bound is load-bearing: 1.56.0 removed BidiAfterInvocationEvent, which
# bedrock-agentcore 1.18.1 imports, so the agent container fails at startup with
# HTTP 424 and every eval scores 0. Raise both pins together, not just this one.
strands-agents[sagemaker]>=1.30.0,<1.56.0
strands-agents-tools==0.2.22
mcp==1.28.1
bedrock-agentcore[strands-agents]==1.18.1
PyJWT[crypto]>=2.10.1
weasyprint>=62.0
markdown>=3.7
# Security pins (transitive)
urllib3>=2.7.0
pillow>=12.0.0
